# HousePrice AI (frontend)

Indian Urban House Price Prediction System, frontend only (React + Vite).
Predictions are mock data for now; see `src/services/predictionService.js`.

## Run it

```bash
npm install
npm run dev
```

Then open the address Vite prints (usually http://localhost:5173).
